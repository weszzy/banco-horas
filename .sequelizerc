// .sequelizerc
const path = require('path');

// __dirname é a raiz do projeto onde .sequelizerc está
module.exports = {
    // Aponta para config/config.js (na raiz)
    config: path.join(__dirname, 'config', 'config.js'),

    // Aponta para src/models (onde seus modelos estão)
    'models-path': path.join(__dirname, 'src', 'models'),

    // Aponta para migrations/ (na raiz, criada pelo init)
    'migrations-path': path.join(__dirname, 'migrations'),

    // Aponta para seeders/ (na raiz, criada pelo init)
    'seeders-path': path.join(__dirname, 'seeders')
};